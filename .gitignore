# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
*.tmp

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Web export build artifacts are committed so CF Pages can deploy with no build step.
# To regenerate: Godot Editor → Export → Web → Export Project → save to web/index.html
